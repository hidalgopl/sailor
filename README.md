# Sailor - SecureAPI CLI tool
This is a security test command line tool to run your SecureAPI tests.


## Build runner locally
`make build`

## Execute tests
`./bin/sailor run --config=example_config.yaml`


